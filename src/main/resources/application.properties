spring.application.name=hanseomate
spring.profiles.default=local
spring.jpa.open-in-view=false
crawler.api.base-url=${CRAWLER_API_BASE_URL:http://34.64.250.12:8000}
crawler.schedule.cron=${CRAWLER_SCHEDULE_CRON:0 0 12,18 * * *}
crawler.schedule.zone=${CRAWLER_SCHEDULE_ZONE:Asia/Seoul}
# Expose only non-sensitive actuator endpoints.
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never